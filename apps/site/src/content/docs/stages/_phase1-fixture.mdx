---
title: "Phase 1 Fixture — Shell Components Demo"
description: "A non-public fixture page that exercises every shell component for FND-05 verification."
stage: 0
slug: phase1-fixture
personas:
  exec: true
  engineer: true
  compliance: true
regulations: []
prerequisites: []
draft: true
---

# Phase 1 fixture page

This page is excluded from the published site (leading underscore in filename). It exists to render every shell component during Phase 1 verification so plan 01-05's components have a host page.

Plan 01-05 fills this page with `<PersonaSection>`, `<RegQuote>`, `<JurisdictionLens>`, `<DiffViewer>`, `<ChangeLog>`, `<GlossaryTerm>`, `<MermaidJourney>`, `<DisclaimerBanner>`, and `<SustainabilityNotice>` references.
