---
phase: 01-foundation-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/anand/AIgov/.gitignore
  - /Users/anand/AIgov/.git/config
  - /Users/anand/AIgov/.gitattributes
  - /Users/anand/AIgov/.nvmrc
  - /Users/anand/AIgov/.npmrc
  - /Users/anand/AIgov/SETUP.md
  - /Users/anand/AIgov/.editorconfig
  - /Users/anand/.gitignore
autonomous: true
requirements: [FND-03]
must_haves:
  truths:
    - "AIgov is a standalone git repository (not nested under /Users/anand/.git)"
    - "Parent home repo no longer tracks /Users/anand/AIgov/ contents"
    - "Node 22.x is enforced via .nvmrc and pnpm is the only package manager"
    - "SETUP.md documents every human-only action required (GitHub repo creation, Cloudflare account, DNS, secret setup)"
  artifacts:
    - path: "/Users/anand/AIgov/.git"
      provides: "Standalone git repo for AIgov project"
    - path: "/Users/anand/AIgov/.nvmrc"
      provides: "Node 22 version pin"
      contains: "22"
    - path: "/Users/anand/AIgov/.npmrc"
      provides: "Enforces pnpm via packageManager and disallows npm/yarn"
    - path: "/Users/anand/AIgov/SETUP.md"
      provides: "Human setup instructions for repo creation, Cloudflare, DNS, secrets"
      min_lines: 100
    - path: "/Users/anand/.gitignore"
      provides: "Excludes AIgov/ from parent home repo"
      contains: "AIgov/"
  key_links:
    - from: "/Users/anand/AIgov/.git/config"
      to: "standalone repo (not parent /Users/anand/.git)"
      via: "git init inside AIgov directory"
      pattern: "core.bare = false"
threat_model:
  surfaces:
    - "Local git config (no secrets stored)"
    - "Parent home directory .gitignore entry"
  mitigations:
    - "Do NOT commit any secrets or .env files; .gitignore covers .env*, *.key, *.pem"
    - "No GitHub remote configured yet — SETUP.md instructs user when ready"
    - "Git user.name/user.email NOT modified globally; respects existing config"
---

<objective>
Phase 1 starts here. The AIgov directory currently sits inside the user's home git repository (/Users/anand/.git). This plan initializes AIgov as its own standalone git repo, removes it from the parent repo's tracking via a .gitignore entry, locks Node 22 + pnpm as the runtime, and writes SETUP.md to document every human-only action (GitHub repo creation, Cloudflare account setup, DNS configuration) so future plans can reference it.

Purpose: Untangle the nested-repo state cleanly before any source code lands. Establish runtime version locks (Node 22, pnpm) that downstream plans (01-02 through 01-08) depend on.

Output: A standalone .git repo for AIgov, parent repo no longer tracks AIgov contents, Node/pnpm version locks committed, SETUP.md created with explicit user-action commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/anand/AIgov/.planning/PROJECT.md
@/Users/anand/AIgov/.planning/STATE.md
@/Users/anand/AIgov/CLAUDE.md
@/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md
@/Users/anand/AIgov/.planning/research/STACK.md
@/Users/anand/AIgov/.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize standalone git repo and untrack from parent home repo</name>
  <files>/Users/anand/AIgov/.git/config, /Users/anand/AIgov/.gitignore, /Users/anand/AIgov/.gitattributes, /Users/anand/.gitignore</files>
  <read_first>
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md (phase boundary, repo state notes)
    - /Users/anand/AIgov/CLAUDE.md (project constraints — no PII tracking, zero-cost target)
    - Existing parent .gitignore: run `cat /Users/anand/.gitignore 2>/dev/null || echo "MISSING"` to see current entries before editing
    - Run `git -C /Users/anand rev-parse --show-toplevel` to confirm parent repo root
    - Run `git -C /Users/anand/AIgov rev-parse --show-toplevel 2>/dev/null` to confirm AIgov is currently nested under parent (no separate .git)
  </read_first>
  <action>
    Step 1 — Append `AIgov/` exclusion to the parent home repo's .gitignore at `/Users/anand/.gitignore` (create the file if it does not exist). Add a comment line above the entry: `# AIgov has its own standalone git repository (initialized 2026-05-24)`. The exclusion line is exactly `AIgov/` (with trailing slash, root-anchored from /Users/anand). Do NOT remove or modify any other lines in the parent .gitignore.

    Step 2 — Untrack AIgov from the parent repo without deleting files. Run from the parent repo root:
    `git -C /Users/anand rm -r --cached AIgov 2>/dev/null || true`
    The `|| true` is required because if AIgov isn't currently tracked by the parent repo, the command errors but is harmless.

    Step 3 — Initialize a standalone git repo inside AIgov:
    `git -C /Users/anand/AIgov init --initial-branch=main`
    Then verify the .git directory exists at `/Users/anand/AIgov/.git/` (not at `/Users/anand/.git`).

    Step 4 — Write `/Users/anand/AIgov/.gitignore` with the following entries (one per line, plain text, no markdown): `node_modules/`, `dist/`, `.astro/`, `.wrangler/`, `.cache/`, `.turbo/`, `coverage/`, `*.log`, `.DS_Store`, `.env`, `.env.*`, `!.env.example`, `*.key`, `*.pem`, `.idea/`, `.vscode/*`, `!.vscode/settings.json.example`, `pnpm-debug.log*`, `dist-ssr/`.

    Step 5 — Write `/Users/anand/AIgov/.gitattributes` to normalize line endings: `* text=auto eol=lf` and `*.{cmd,[cC][mM][dD]} text eol=crlf` and `*.{bat,[bB][aA][tT]} text eol=crlf`.

    Step 6 — Verify the initial branch name is `main` (not `master`). If git defaulted to `master`, rename: `git -C /Users/anand/AIgov branch -m master main`. Do NOT modify the user's global git config.
  </action>
  <verify>
    <automated>test -d /Users/anand/AIgov/.git && test ! -f /Users/anand/AIgov/.git/HEAD || (cat /Users/anand/AIgov/.git/HEAD | grep -q "ref: refs/heads/main") && grep -q "^AIgov/" /Users/anand/.gitignore && test -f /Users/anand/AIgov/.gitignore && test -f /Users/anand/AIgov/.gitattributes && git -C /Users/anand/AIgov rev-parse --show-toplevel | grep -q "/Users/anand/AIgov$"</automated>
  </verify>
  <done>
    - `/Users/anand/AIgov/.git/` directory exists and HEAD points to `refs/heads/main`
    - `git -C /Users/anand/AIgov rev-parse --show-toplevel` outputs `/Users/anand/AIgov`
    - Parent home repo `/Users/anand/.gitignore` contains line `AIgov/` (root-anchored, trailing slash)
    - `/Users/anand/AIgov/.gitignore` and `/Users/anand/AIgov/.gitattributes` exist with the entries above
    - Parent repo `git -C /Users/anand status --ignored | grep AIgov` shows AIgov as ignored (not tracked)
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock Node 22 + pnpm runtime and write SETUP.md with human-action checklist</name>
  <files>/Users/anand/AIgov/.nvmrc, /Users/anand/AIgov/.npmrc, /Users/anand/AIgov/.editorconfig, /Users/anand/AIgov/SETUP.md</files>
  <read_first>
    - /Users/anand/AIgov/CLAUDE.md (Recommended Stack section — Node 22 + pnpm + Astro 6 + Cloudflare)
    - /Users/anand/AIgov/.planning/research/STACK.md (Version Compatibility table — Node `>=20.19` for Astro 6, set in `.nvmrc`)
    - /Users/anand/AIgov/.planning/research/ARCHITECTURE.md (Repository Layout — `.nvmrc` listed at root)
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md (Operational Cadence section — HEARTBEAT_WEBHOOK_URL secret name; Cloudflare proxy/SSL/cache decisions; SITE_DOMAIN placeholder)
    - /Users/anand/AIgov/.planning/research/PITFALLS.md (Pitfall 9 — Cloudflare orange-cloud requirement)
  </read_first>
  <action>
    Step 1 — Write `/Users/anand/AIgov/.nvmrc` containing the single line `22` (no `v` prefix, no trailing version specifier; pnpm and corepack pick the latest 22.x LTS).

    Step 2 — Write `/Users/anand/AIgov/.npmrc` with these exact lines:
    `engine-strict=true`
    `auto-install-peers=true`
    `node-linker=isolated`
    `prefer-workspace-packages=true`
    `save-exact=true`

    The `engine-strict=true` enforces the `engines` field on package.json (added in plan 01-02). `node-linker=isolated` is the pnpm default and is set explicitly here to prevent npm/yarn fallback. `save-exact=true` matches the version-pin discipline from STACK.md.

    Step 3 — Write `/Users/anand/AIgov/.editorconfig` with: root file marker (`root = true`), then `[*]` block with `indent_style = space`, `indent_size = 2`, `end_of_line = lf`, `charset = utf-8`, `trim_trailing_whitespace = true`, `insert_final_newline = true`. Then a `[*.md]` block overriding `trim_trailing_whitespace = false` (Markdown trailing-space line breaks).

    Step 4 — Write `/Users/anand/AIgov/SETUP.md` documenting every human-only action required for Phase 1 to reach a "live URL" state. The file MUST contain these sections in order (use level-2 headings exactly):
    1. `## Prerequisites` — Node 22 via nvm (`nvm install 22 && nvm use`), pnpm via corepack (`corepack enable && corepack prepare pnpm@latest --activate`).
    2. `## Local Development` — `pnpm install`, `pnpm dev` (after plan 01-03 lands the Astro site).
    3. `## GitHub Repository Setup` — exact `gh repo create` command: `gh repo create AIgov --private --source=. --remote=origin --description "Public AI Governance knowledge site"`. Note: keeps repo PRIVATE until Phase 6 per PROJECT.md.
    4. `## GitHub Pages Configuration` — instructions to enable Pages in repo settings (Source: GitHub Actions); custom domain field stays empty until DNS is configured.
    5. `## Cloudflare Account & DNS` — per CONTEXT.md decisions: (a) create Cloudflare account, (b) add `SITE_DOMAIN` placeholder zone (final domain TBD), (c) **orange-cloud proxy mode is required** (Pitfall 9 — bandwidth shielding does not work in DNS-only mode), (d) Universal SSL set to **Full (strict)**, (e) Cache Rules: `Cache-Control: public, max-age=31536000, immutable` on `/_astro/*` hashed assets, `max-age=300, must-revalidate` on HTML routes.
    6. `## Required GitHub Secrets` — exactly one secret in Phase 1: `HEARTBEAT_WEBHOOK_URL` (optional — Slack/Discord/Pushover webhook for heartbeat failures per FND-09). Cloudflare API token and Anthropic key are Phase 3/5 scope; do not configure in Phase 1.
    7. `## Domain Switch Procedure` — When the user acquires `SITE_DOMAIN`, exactly one config flip: edit `apps/site/src/config/site.ts` `SITE_DOMAIN` constant (defined in plan 01-06) and update DNS records on Cloudflare.
    8. `## What Phase 1 Ships vs Requires Human Action` — Table with two columns: "Automated (Claude does)" vs "Manual (you must do)". Phase 1 automated rows: monorepo scaffold, Astro+Starlight site, content collections, shell components, CI workflows, keepalive/heartbeat workflows, deploy workflow. Manual rows: `gh repo create`, GitHub Pages enable, Cloudflare account create, Cloudflare proxy mode toggle, Cloudflare cache rules, DNS records when domain acquired, set `HEARTBEAT_WEBHOOK_URL` secret if Slack/Discord notifications desired.

    Each section must be self-contained — paste-into-shell commands where applicable. No "see docs" punts.
  </action>
  <verify>
    <automated>test -f /Users/anand/AIgov/.nvmrc && grep -q "^22$" /Users/anand/AIgov/.nvmrc && test -f /Users/anand/AIgov/.npmrc && grep -q "engine-strict=true" /Users/anand/AIgov/.npmrc && test -f /Users/anand/AIgov/.editorconfig && grep -q "root = true" /Users/anand/AIgov/.editorconfig && test -f /Users/anand/AIgov/SETUP.md && grep -q "## Prerequisites" /Users/anand/AIgov/SETUP.md && grep -q "## GitHub Repository Setup" /Users/anand/AIgov/SETUP.md && grep -q "## Cloudflare Account" /Users/anand/AIgov/SETUP.md && grep -q "HEARTBEAT_WEBHOOK_URL" /Users/anand/AIgov/SETUP.md && grep -q "orange-cloud" /Users/anand/AIgov/SETUP.md && grep -q "Full (strict)" /Users/anand/AIgov/SETUP.md && grep -q "gh repo create" /Users/anand/AIgov/SETUP.md && wc -l /Users/anand/AIgov/SETUP.md | awk '{if ($1 < 100) exit 1}'</automated>
  </verify>
  <done>
    - `.nvmrc` contains `22` (no v prefix, no minor)
    - `.npmrc` enforces `engine-strict=true` and pnpm-only conventions
    - `.editorconfig` exists with 2-space, LF, UTF-8, final-newline defaults
    - `SETUP.md` exists, ≥100 lines, with all 8 required sections, contains exact `gh repo create AIgov --private --source=.` command, references HEARTBEAT_WEBHOOK_URL secret, documents orange-cloud + Full Strict + cache rules
    - All paste-into-shell commands work without modification
  </done>
</task>

</tasks>

<verification>
- Run `git -C /Users/anand/AIgov status` — should show untracked files only (no warning about being inside parent repo, no nested-repo warning)
- Run `git -C /Users/anand status --ignored | grep AIgov` — should show AIgov as ignored at the parent level
- Run `cat /Users/anand/AIgov/.nvmrc` — should output `22`
- Run `cat /Users/anand/AIgov/SETUP.md | head -50` — should clearly show prerequisites and manual-action sections
- Open SETUP.md in a text reader — sections must be paste-and-run, no ambiguous prose
</verification>

<success_criteria>
- AIgov is a standalone git repo with `main` as the initial branch
- Parent home repo no longer tracks AIgov (verified via `git status --ignored`)
- Node 22 + pnpm locked via `.nvmrc` and `.npmrc engine-strict=true`
- `.editorconfig` enforces 2-space LF UTF-8 across all file types
- SETUP.md contains 8 required sections including the exact `gh repo create` command, Cloudflare orange-cloud requirement, Full Strict SSL, cache rule snippets, and HEARTBEAT_WEBHOOK_URL secret reference
- Phase 1 can proceed to plan 01-02 (monorepo scaffold) without parent-repo conflicts
</success_criteria>

<output>
After completion, create `/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-01-SUMMARY.md` documenting: standalone repo path, parent .gitignore entry added, Node version pinned, SETUP.md section list, any deviations from the plan.
</output>
