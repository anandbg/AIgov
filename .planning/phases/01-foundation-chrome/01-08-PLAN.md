---
phase: 01-foundation-chrome
plan: 08
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-05
  - 01-06
files_modified:
  - /Users/anand/AIgov/.github/workflows/ci-quality.yml
  - /Users/anand/AIgov/.github/workflows/ci-axe.yml
  - /Users/anand/AIgov/.github/workflows/ci-weight-budget.yml
  - /Users/anand/AIgov/.lighthouserc.cjs
  - /Users/anand/AIgov/scripts/check-weight-budget.mjs
  - /Users/anand/AIgov/scripts/run-axe.mjs
  - /Users/anand/AIgov/package.json
  - /Users/anand/AIgov/apps/site/package.json
autonomous: true
requirements:
  - FND-10
must_haves:
  truths:
    - "Pushing a PR that exceeds the LCP/INP/CLS budgets fails the ci-quality workflow and blocks merge"
    - "Pushing a PR that fails axe-core WCAG 2.2 AA checks fails ci-axe and blocks merge"
    - "Pushing a PR that exceeds image-weight or total-page-weight budgets fails ci-weight-budget and blocks merge"
    - "All three workflows can be run locally via root pnpm scripts (lighthouse, axe, weight-budget) before pushing"
    - "Budget thresholds are documented and tunable in `.lighthouserc.cjs` (LH-CI) and `scripts/check-weight-budget.mjs` (weight budgets)"
  artifacts:
    - path: "/Users/anand/AIgov/.lighthouserc.cjs"
      provides: "Lighthouse-CI config with LCP/INP/CLS budgets per UI-SPEC + Web Vitals 2026 thresholds"
      contains: "lhci"
    - path: "/Users/anand/AIgov/.github/workflows/ci-quality.yml"
      provides: "Runs Lighthouse-CI against built site on every PR"
    - path: "/Users/anand/AIgov/.github/workflows/ci-axe.yml"
      provides: "Runs axe-core against built site on every PR; light + dark + landing + about + fixture stage pages"
    - path: "/Users/anand/AIgov/.github/workflows/ci-weight-budget.yml"
      provides: "Asserts per-image and total-page-weight budgets"
    - path: "/Users/anand/AIgov/scripts/check-weight-budget.mjs"
      provides: "Node script that walks dist/, asserts image and total HTML+JS+CSS sizes against budget"
    - path: "/Users/anand/AIgov/scripts/run-axe.mjs"
      provides: "Node script: uses playwright's built-in static server to serve dist/, runs axe-core against landing + about + fixture pages in light + dark"
  key_links:
    - from: ".lighthouserc.cjs"
      to: "apps/site/dist (served via lhci built-in static server)"
      via: "lhci collect with --staticDistDir"
      pattern: "staticDistDir"
    - from: "scripts/run-axe.mjs"
      to: "apps/site/dist + playwright + axe-core"
      via: "playwright serveHandler over dist/, axe-core injection per page"
      pattern: "@axe-core/playwright"
    - from: "scripts/check-weight-budget.mjs"
      to: "apps/site/dist file sizes"
      via: "fs/promises stat walking dist/"
      pattern: "stat\\("
threat_model:
  surfaces:
    - "CI workflows run untrusted PR code on GitHub-hosted runners (standard GH Actions threat model)"
    - "Playwright + axe-core packages (dev dependencies — included only at CI time, not in production bundle)"
    - "Lighthouse-CI runs against a local static-served dist (no external network during PR runs)"
  mitigations:
    - "All three workflows declare `permissions: contents: read` only (no write access to repo)"
    - "Path filters scope each workflow so it only runs when relevant files change"
    - "axe-core + playwright pinned to specific versions; updates flow through Dependabot"
    - "No secrets accessed by these workflows"
    - "Scripts use Node's built-in `node:http` static-serve helper or playwright's internal server — no shell exec, no child_process spawn of external commands with user input"
---

<objective>
Land the three CI quality gates that block merges on regressions: Lighthouse-CI (LCP/INP/CLS budgets), axe-core (WCAG 2.2 AA), and weight-budget (image + total page weight). Per UI-SPEC line 633 and CONTEXT.md "Claude's Discretion" — Lighthouse defaults as baseline (LCP < 2.5s, INP < 200ms, CLS < 0.1); image-weight < 100KB/image and total page weight < 500KB as opening figures, tunable after Phase 2 content lands.

Purpose: ROADMAP.md Phase 1 Success Criterion 3 requires: "Pushing a PR that exceeds the Lighthouse LCP/INP/CLS budgets, fails axe-core checks, or exceeds the image/page-weight budget visibly fails CI and blocks merge." This plan delivers that gate so Phase 2 content authoring works against real CI, not stubs.

Output: Three CI workflows + root-level pnpm scripts so the same checks can run locally before pushing. Lighthouse + axe + weight budgets are all green against the Phase 1 shell (only content is the fixture page + about + landing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-UI-SPEC.md
@/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md
@/Users/anand/AIgov/.planning/research/PITFALLS.md
@/Users/anand/AIgov/apps/site/package.json
@/Users/anand/AIgov/.github/workflows/deploy-site.yml
@/Users/anand/AIgov/MAINTENANCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lighthouse-CI config + workflow + local pnpm script</name>
  <files>/Users/anand/AIgov/.lighthouserc.cjs, /Users/anand/AIgov/.github/workflows/ci-quality.yml, /Users/anand/AIgov/package.json</files>
  <read_first>
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-UI-SPEC.md (Implementation Verification line 633 — Lighthouse-CI green on landing + About + fixture stage)
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md (Claude's Discretion section — LCP < 2.5s, INP < 200ms, CLS < 0.1 baseline; tune downward after content lands)
    - /Users/anand/AIgov/.planning/research/PITFALLS.md (Pitfall M-6 — Lighthouse score regresses quietly)
    - /Users/anand/AIgov/apps/site/astro.config.mjs (build output `apps/site/dist/`)
    - /Users/anand/AIgov/.github/workflows/deploy-site.yml (Node setup pattern — copy for consistency)
    - Lighthouse-CI 2026 docs for `lhci collect --staticDistDir` and budgets/assertions API
  </read_first>
  <action>
    Step 1 — Add Lighthouse-CI as a dev dependency at the repo root. Edit `/Users/anand/AIgov/package.json` and add to `devDependencies`: `"@lhci/cli": "0.14.x"` (pin exact via `save-exact=true`). Also add a `"lighthouse"` script in scripts: `"lighthouse": "lhci autorun"`. Run `pnpm install` to update lockfile.

    Step 2 — Create `/Users/anand/AIgov/.lighthouserc.cjs` (CommonJS — lhci 0.14 expects .cjs):

    The file body must export `{ ci: { collect, assert, upload } }` per the lhci API. Configure:
    - `collect.staticDistDir`: `./apps/site/dist`
    - `collect.url`: `['http://localhost/index.html', 'http://localhost/about/index.html']`
    - `collect.numberOfRuns`: 3 (reduces flake; lhci picks median)
    - `collect.settings.preset`: `'desktop'` (Phase 1 baseline; mobile preset added in Phase 2)
    - `assert.assertions` (per CONTEXT.md Claude's Discretion):
      - `'largest-contentful-paint': ['error', { maxNumericValue: 2500 }]`
      - `'cumulative-layout-shift': ['error', { maxNumericValue: 0.1 }]`
      - `'interaction-to-next-paint': ['warn', { maxNumericValue: 200 }]` — warn-only Phase 1 because INP requires real interaction; promote to error in Phase 2 when content lands
      - `'first-contentful-paint': ['warn', { maxNumericValue: 1800 }]`
      - `'total-blocking-time': ['warn', { maxNumericValue: 200 }]`
      - `'speed-index': ['warn', { maxNumericValue: 3400 }]`
      - `'uses-https': ['off']`, `'is-on-https': ['off']`, `'redirects-http': ['off']` — static dist served via localhost in CI
      - `'errors-in-console': ['error']` — no console errors allowed
      - `'no-document-write': ['error']`
      - `'no-vulnerable-libraries': ['error']`
      - `'meta-description': ['warn']`
      - `'document-title': ['error']`
      - `'html-has-lang': ['error']` — `lang="en"` set in Phase 1 (UI-SPEC line 481)
    - `upload.target`: `'temporary-public-storage'`

    Add a docstring header to the file explaining: Phase 1 baseline budgets per CONTEXT.md "Claude's Discretion"; tune after Phase 2 content lands; pages tested in Phase 1 are landing and /about/.

    Step 3 — Create `/Users/anand/AIgov/.github/workflows/ci-quality.yml`:
    ```yaml
    name: CI — Lighthouse

    on:
      pull_request:
        paths:
          - 'apps/site/**'
          - 'packages/shared/**'
          - 'pnpm-workspace.yaml'
          - 'package.json'
          - 'pnpm-lock.yaml'
          - '.lighthouserc.cjs'
          - '.github/workflows/ci-quality.yml'
      workflow_dispatch:

    concurrency:
      group: ci-quality-${{ github.ref }}
      cancel-in-progress: true

    permissions:
      contents: read

    jobs:
      lighthouse:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: pnpm/action-setup@v4
            with: { run_install: false }
          - uses: actions/setup-node@v4
            with:
              node-version-file: .nvmrc
              cache: pnpm
          - run: pnpm install --frozen-lockfile
          - name: Build site
            run: pnpm --filter @aigov/site build
          - name: Lighthouse CI
            run: pnpm lighthouse
            env:
              LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
    ```

    Step 4 — Validate locally: run `pnpm install` then `pnpm --filter @aigov/site build` then `pnpm lighthouse` from repo root. Lighthouse-CI should pick up `.lighthouserc.cjs`, serve `apps/site/dist/` on a temp port, run 3 trials, and report results. Phase 1 baseline is small (static Starlight site, no images yet) — all `error`-level assertions should pass.

    Step 5 — If Lighthouse flags any `error` assertion, investigate and either:
    (a) Fix the underlying issue (e.g., add a missing meta description),
    (b) If the issue is a known Phase 1 expectation, document the rationale in the lhci config with a comment and downgrade to `'warn'`.

    Do NOT disable an assertion to make CI green — that defeats the purpose. The acceptable path is fix or document with rationale.
  </action>
  <verify>
    <automated>test -f /Users/anand/AIgov/.lighthouserc.cjs && grep -q "largest-contentful-paint" /Users/anand/AIgov/.lighthouserc.cjs && grep -q "maxNumericValue: 2500" /Users/anand/AIgov/.lighthouserc.cjs && grep -q "cumulative-layout-shift" /Users/anand/AIgov/.lighthouserc.cjs && grep -q "interaction-to-next-paint" /Users/anand/AIgov/.lighthouserc.cjs && grep -q "errors-in-console" /Users/anand/AIgov/.lighthouserc.cjs && grep -q "staticDistDir" /Users/anand/AIgov/.lighthouserc.cjs && test -f /Users/anand/AIgov/.github/workflows/ci-quality.yml && grep -q "lhci\|lighthouse" /Users/anand/AIgov/.github/workflows/ci-quality.yml && grep -q "permissions:" /Users/anand/AIgov/.github/workflows/ci-quality.yml && python3 -c "import yaml; yaml.safe_load(open('/Users/anand/AIgov/.github/workflows/ci-quality.yml'))" && node -e "const p=require('/Users/anand/AIgov/package.json'); if(!p.scripts.lighthouse) process.exit(1); if(!p.devDependencies['@lhci/cli']) process.exit(1);" && cd /Users/anand/AIgov && pnpm install --frozen-lockfile 2>&1 | tail -3 && pnpm --filter @aigov/site build 2>&1 | grep -qE "(Complete|built in|✓)" && pnpm lighthouse 2>&1 | tee /tmp/lhci.log | grep -qE "(passed|All assertions passed|all results)"</automated>
  </verify>
  <done>
    - `.lighthouserc.cjs` exists with LCP/CLS as `error` thresholds, INP/FCP/TBT/SI as `warn`, plus best-practice gates (errors-in-console, no-vulnerable-libraries)
    - `ci-quality.yml` workflow runs on PRs touching site, has `contents: read` only, uses Node 22 + pnpm
    - Root `package.json` has `"lighthouse": "lhci autorun"` script and `@lhci/cli` dev dep
    - Running `pnpm lighthouse` locally against the Phase 1 build passes all error-level assertions
    - YAML parses cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: axe-core WCAG 2.2 AA workflow + weight-budget script + workflows</name>
  <files>/Users/anand/AIgov/scripts/run-axe.mjs, /Users/anand/AIgov/scripts/check-weight-budget.mjs, /Users/anand/AIgov/.github/workflows/ci-axe.yml, /Users/anand/AIgov/.github/workflows/ci-weight-budget.yml, /Users/anand/AIgov/apps/site/package.json, /Users/anand/AIgov/MAINTENANCE.md</files>
  <read_first>
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-UI-SPEC.md (Accessibility Contract section lines 460-490 — WCAG 2.2 AA criteria to verify, light + dark, landing + About + fixture stage pages; Implementation Verification lines 632-643)
    - /Users/anand/AIgov/.planning/research/PITFALLS.md (m-3 — mobile font sizes)
    - /Users/anand/AIgov/apps/site/package.json (already has @astrojs/check; add @axe-core/playwright and playwright as dev deps)
    - /Users/anand/AIgov/apps/site/dist/ (built site directory — exists after pnpm --filter @aigov/site build)
    - axe-core + @axe-core/playwright 2026 docs
  </read_first>
  <action>
    Step 1 — Add dev deps. Edit `/Users/anand/AIgov/apps/site/package.json` `devDependencies`:
    - `"@axe-core/playwright": "4.x"` (pin exact latest 4.x)
    - `"playwright": "1.48.x"` (matches STACK.md)
    - `"axe-core": "4.x"` (transitively required by @axe-core/playwright but pin explicitly)

    Add scripts to `apps/site/package.json`:
    - `"axe": "node ../../scripts/run-axe.mjs"`

    Add a root-level pnpm script in `/Users/anand/AIgov/package.json` scripts:
    - `"axe": "pnpm --filter @aigov/site axe"`
    - `"weight-budget": "node ./scripts/check-weight-budget.mjs apps/site/dist"`

    Run `pnpm install` to pull dev deps. Then `pnpm exec playwright install chromium` to install the headless browser binary (done once per machine; CI re-installs).

    Step 2 — Create directory `/Users/anand/AIgov/scripts/` and write `/Users/anand/AIgov/scripts/run-axe.mjs`. The script uses Node's built-in `node:http` to serve dist/ statically (no external `serve` binary needed, no child_process), launches playwright's chromium, navigates to each page, toggles theme via `localStorage`, and runs axe-core.

    Script body MUST include:
    1. Import `createServer` from `node:http`, `readFile`, `stat` from `node:fs/promises`, `extname`, `join`, `resolve` from `node:path`
    2. Import `chromium` from `playwright` and `AxeBuilder` from `@axe-core/playwright`
    3. Configuration constants at top: `PORT = 4321`, `DIST_DIR = resolve('apps/site/dist')`, `PATHS = ['/', '/about/']`, `OPTIONAL_PATHS = ['/_phase1-fixture/']`, `WCAG_TAGS = ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa']`
    4. A `startServer()` async function that creates an HTTP server serving files from DIST_DIR. For each request:
       - If pathname ends with `/`, append `index.html`
       - Resolve safely (prevent `..` traversal): `const filePath = resolve(DIST_DIR, '.' + pathname); if (!filePath.startsWith(DIST_DIR)) return 403`
       - Read file, set content-type by extension (html, css, js, svg, png, jpg, jpeg, gif, webp, woff, woff2, json), return 200; on ENOENT return 404
       - Return a Promise that resolves to the server instance when it's listening on PORT
    5. A `runAxe(page, url, theme)` async function that navigates to `url`, sets `data-theme` attribute on `<html>` via `page.evaluate`, runs `new AxeBuilder({ page }).withTags(WCAG_TAGS).analyze()`, returns `results.violations`
    6. A `main()` async function:
       - Start server via `startServer()`
       - Launch chromium, new context, new page
       - For each path in `[...PATHS, ...OPTIONAL_PATHS]`:
         - HEAD-fetch to confirm 200; if OPTIONAL and 404, log SKIP and continue; if required and not 200, throw
         - For each theme in `['light', 'dark']`: run axe, accumulate violations, log per-page summary
       - Close browser, close server
       - If any violations, log details (impact, id, help, top 3 nodes) and `process.exit(1)`; else log success
    7. `main().catch((e) => { console.error(e); process.exit(1); })`

    The static server is implemented with Node's built-in `node:http` — no external subprocess, no shell exec. This satisfies the security threat model (no child_process spawn of arbitrary commands).

    Step 3 — Create `/Users/anand/AIgov/scripts/check-weight-budget.mjs`. Walks the dist/ directory, sums HTML+CSS+JS sizes per page, and asserts both image-weight and total-page-weight budgets.

    Budgets per CONTEXT.md "Claude's Discretion":
    - Per-image: `PER_IMAGE_KB = 100` (constant at top of file)
    - Per-page total: `PER_PAGE_KB = 500` (constant at top of file)

    Script body MUST:
    1. Import `readFile`, `readdir`, `stat` from `node:fs/promises` and `path` from `node:path`
    2. Take `ROOT` from `process.argv[2] || 'apps/site/dist'`
    3. Async generator `walk(dir)` yields every file path under dir (using `readdir({ withFileTypes: true })` and recursion)
    4. `isImage(file)`: regex match `\.(png|jpe?g|gif|webp|svg|avif)$/i`
    5. `refsFromHtml(html, htmlDir)`: extract `href=` and `src=` values via regex; ignore absolute `https?:`, `data:`, `mailto:`; resolve relative paths against htmlDir, absolute against ROOT
    6. `main()`:
       - Walk dist, collect all files
       - Per-image budget loop: for each image file, stat size, log ok/fail with KB, increment failures on overage
       - Per-page budget loop: for each `.html` file, read content, extract refs, sum size of html + each ref's file size (skip missing refs gracefully)
       - Log totals; exit 1 if any failures
    7. `main().catch((e) => { console.error(e); process.exit(1); })`

    Step 4 — Create `/Users/anand/AIgov/.github/workflows/ci-axe.yml`:
    ```yaml
    name: CI — axe (WCAG 2.2 AA)

    on:
      pull_request:
        paths:
          - 'apps/site/**'
          - 'packages/shared/**'
          - 'scripts/run-axe.mjs'
          - '.github/workflows/ci-axe.yml'
      workflow_dispatch:

    concurrency:
      group: ci-axe-${{ github.ref }}
      cancel-in-progress: true

    permissions:
      contents: read

    jobs:
      axe:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: pnpm/action-setup@v4
            with: { run_install: false }
          - uses: actions/setup-node@v4
            with:
              node-version-file: .nvmrc
              cache: pnpm
          - run: pnpm install --frozen-lockfile
          - name: Install playwright browser (chromium)
            run: pnpm exec playwright install --with-deps chromium
            working-directory: apps/site
          - name: Build site
            run: pnpm --filter @aigov/site build
          - name: Run axe
            run: pnpm axe
    ```

    Step 5 — Create `/Users/anand/AIgov/.github/workflows/ci-weight-budget.yml`:
    ```yaml
    name: CI — weight budget

    on:
      pull_request:
        paths:
          - 'apps/site/**'
          - 'packages/shared/**'
          - 'scripts/check-weight-budget.mjs'
          - '.github/workflows/ci-weight-budget.yml'
      workflow_dispatch:

    concurrency:
      group: ci-weight-${{ github.ref }}
      cancel-in-progress: true

    permissions:
      contents: read

    jobs:
      weight:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: pnpm/action-setup@v4
            with: { run_install: false }
          - uses: actions/setup-node@v4
            with:
              node-version-file: .nvmrc
              cache: pnpm
          - run: pnpm install --frozen-lockfile
          - name: Build site
            run: pnpm --filter @aigov/site build
          - name: Check weight budgets
            run: pnpm weight-budget
    ```

    Step 6 — Run all three checks locally to confirm green baseline:
    - `pnpm install` to wire deps
    - `pnpm --filter @aigov/site build`
    - `pnpm lighthouse` (Task 1)
    - `pnpm axe`
    - `pnpm weight-budget`

    If any fails, fix root cause (add missing meta description, fix axe issue, reduce a too-large image). Document any tunings of thresholds in SUMMARY.

    Step 7 — Update `/Users/anand/AIgov/MAINTENANCE.md` to add a `## CI Gates` section listing the three workflows (LH, axe, weight) and their budget thresholds. Future maintainers tuning budgets edit `.lighthouserc.cjs` and the constants at top of `scripts/check-weight-budget.mjs`.
  </action>
  <verify>
    <automated>test -f /Users/anand/AIgov/scripts/run-axe.mjs && grep -q "@axe-core/playwright" /Users/anand/AIgov/scripts/run-axe.mjs && grep -q "wcag22aa" /Users/anand/AIgov/scripts/run-axe.mjs && grep -q "data-theme" /Users/anand/AIgov/scripts/run-axe.mjs && grep -q "node:http\|createServer" /Users/anand/AIgov/scripts/run-axe.mjs && test -f /Users/anand/AIgov/scripts/check-weight-budget.mjs && grep -q "PER_IMAGE_KB = 100" /Users/anand/AIgov/scripts/check-weight-budget.mjs && grep -q "PER_PAGE_KB = 500" /Users/anand/AIgov/scripts/check-weight-budget.mjs && test -f /Users/anand/AIgov/.github/workflows/ci-axe.yml && test -f /Users/anand/AIgov/.github/workflows/ci-weight-budget.yml && python3 -c "import yaml; yaml.safe_load(open('/Users/anand/AIgov/.github/workflows/ci-axe.yml'))" && python3 -c "import yaml; yaml.safe_load(open('/Users/anand/AIgov/.github/workflows/ci-weight-budget.yml'))" && node -e "const p=require('/Users/anand/AIgov/package.json'); if(!p.scripts.axe) process.exit(1); if(!p.scripts['weight-budget']) process.exit(1);" && node -e "const p=require('/Users/anand/AIgov/apps/site/package.json'); if(!p.devDependencies['@axe-core/playwright']) process.exit(1); if(!p.devDependencies.playwright) process.exit(1);" && cd /Users/anand/AIgov && pnpm install --frozen-lockfile 2>&1 | tail -3 && pnpm --filter @aigov/site build 2>&1 | grep -qE "(Complete|built in|✓)" && pnpm weight-budget 2>&1 | tee /tmp/weight.log | grep -q "All budgets within limits" && pnpm exec playwright install chromium 2>&1 | tail -3 && pnpm axe 2>&1 | tee /tmp/axe.log | grep -q "passed axe-core WCAG 2.2 AA" && grep -q "CI Gates" /Users/anand/AIgov/MAINTENANCE.md</automated>
  </verify>
  <done>
    - `scripts/run-axe.mjs` exists; serves dist/ via built-in `node:http`; runs axe-core on landing + about + fixture in both themes; uses WCAG 2.2 AA tags
    - `scripts/check-weight-budget.mjs` exists; budgets `PER_IMAGE_KB = 100`, `PER_PAGE_KB = 500` at top of file (tunable constants)
    - Two new workflows: `ci-axe.yml` and `ci-weight-budget.yml`; both have least-privilege permissions
    - Local `pnpm axe` and `pnpm weight-budget` both pass against Phase 1 baseline
    - All three CI workflows parse cleanly via Python yaml
    - MAINTENANCE.md `## CI Gates` section added documenting thresholds and tuning location
  </done>
</task>

</tasks>

<verification>
- `pnpm lighthouse` exits 0 against the Phase 1 build (landing + about pages)
- `pnpm axe` exits 0 against the Phase 1 build (light + dark themes, all critical pages)
- `pnpm weight-budget` exits 0 against the Phase 1 build (all images ≤100KB, all pages ≤500KB)
- Three CI workflow files parse cleanly and have least-privilege permissions
- Path filters on each workflow ensure they only run when relevant files change (avoids wasting Actions minutes)
- The same checks runnable locally before pushing — fast feedback loop
</verification>

<success_criteria>
- FND-10: LCP/INP/CLS budgets enforced via Lighthouse-CI on every PR with paths in apps/site; axe-core WCAG 2.2 AA enforced via dedicated workflow against light + dark themes on landing + about + fixture stage; image-weight and total-page-weight budgets enforced via custom script
- Pitfall M-6 mitigation: Lighthouse regressions caught BEFORE merge, not later
- UI-SPEC Implementation Verification line 633-636 satisfied: Lighthouse-CI green on landing + About + fixture stage; light + dark both pass axe-core
- ROADMAP.md Phase 1 Success Criterion 3 fully satisfied: PR exceeding any budget visibly fails CI and blocks merge
- All three checks runnable locally via root-level pnpm scripts — fast feedback for developers
- Budget thresholds are documented and easily tunable (constants at top of script files; lhci config in single .cjs)
</success_criteria>

<output>
After completion, create `/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-08-SUMMARY.md` documenting: final budget thresholds (LCP/CLS/INP + image + page-weight), Phase 1 baseline numbers (actual LCP/CLS/INP values + actual largest image + heaviest page from local runs), workflow path filters, any axe-core rules disabled (with rationale), pnpm script inventory.
</output>
