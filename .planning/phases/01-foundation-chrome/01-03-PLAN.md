---
phase: 01-foundation-chrome
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - /Users/anand/AIgov/apps/site/package.json
  - /Users/anand/AIgov/apps/site/tsconfig.json
  - /Users/anand/AIgov/apps/site/astro.config.mjs
  - /Users/anand/AIgov/apps/site/src/env.d.ts
  - /Users/anand/AIgov/apps/site/src/styles/global.css
  - /Users/anand/AIgov/apps/site/public/favicon.svg
  - /Users/anand/AIgov/apps/site/public/.nojekyll
  - /Users/anand/AIgov/apps/site/src/pages/index.astro
  - /Users/anand/AIgov/apps/site/src/pages/_placeholder.md
autonomous: true
requirements:
  - FND-02
  - SRC-01
  - SRC-02
must_haves:
  truths:
    - "`pnpm --filter @aigov/site dev` starts Astro dev server on http://localhost:4321 and the root route returns 200"
    - "`pnpm --filter @aigov/site build` produces a static dist/ directory with HTML for the index route"
    - "Pagefind search modal is wired (Starlight built-in) and opens with ⌘K/Ctrl+K"
    - "Pagefind indexes all stage pages, regulation pages, vendor pages, and glossary entries (SRC-02 — once content lands in plan 01-04, search includes those collections)"
    - "Sidebar shows the 12-stage spine placeholder (12 items, ordered) on every page (FND-06)"
    - "No web fonts are fetched (CSS uses system-ui stack only)"
    - "Dark mode toggle is present in the Starlight header and respects prefers-color-scheme by default"
  artifacts:
    - path: "/Users/anand/AIgov/apps/site/astro.config.mjs"
      provides: "Astro 6 config with Starlight + Tailwind v4 + astro-mermaid + astro-icon integrations"
      contains: "starlight"
    - path: "/Users/anand/AIgov/apps/site/package.json"
      provides: "Site workspace manifest, depends on astro 6.0.x, @astrojs/starlight 0.39.x, tailwindcss 4.1.x, @tailwindcss/vite, @astrojs/starlight-tailwind, astro-mermaid, astro-icon, @iconify-json/lucide, pagefind, @aigov/shared workspace:*"
      contains: "@astrojs/starlight"
    - path: "/Users/anand/AIgov/apps/site/src/styles/global.css"
      provides: "Tailwind v4 import + @theme block with slate-indigo accent + system font stacks + reduced-motion default"
      min_lines: 60
    - path: "/Users/anand/AIgov/apps/site/src/pages/index.astro"
      provides: "Landing page with placeholder Mermaid + intro text"
  key_links:
    - from: "apps/site/astro.config.mjs"
      to: "Starlight 0.39 integration with sidebar config listing 12 stages"
      via: "starlight({ sidebar: [...] }) call"
      pattern: "starlight\\("
    - from: "apps/site/src/styles/global.css"
      to: "Tailwind v4 + Starlight CSS variables"
      via: "@import 'tailwindcss'; @import '@astrojs/starlight-tailwind'; @theme block"
      pattern: "@import \"tailwindcss\""
    - from: "apps/site/package.json"
      to: "@aigov/shared via workspace:*"
      via: "dependencies entry"
      pattern: "\"@aigov/shared\": \"workspace:\\*\""
threat_model:
  surfaces:
    - "Astro/Starlight dependency chain (supply chain — pin exact versions)"
    - "Public static asset directory (no secrets)"
    - "Tailwind/Vite build pipeline"
  mitigations:
    - "All dependencies version-pinned (save-exact=true from plan 01-01)"
    - "No runtime API keys in site build (Phase 1 is purely static)"
    - "favicon.svg is hand-authored inline SVG (no external CDN)"
    - "No external font CDN, no GA, no third-party scripts — verified by Network tab inspection in DOD"
---

<objective>
Scaffold `apps/site` as an Astro 6 + Starlight 0.39 + Tailwind v4 site with the 12-stage sidebar placeholder, Pagefind search (⌘K shortcut), system-font typography, slate-indigo accent CSS theme, and Mermaid + Icon integrations. This is the visual shell FND-02 requires and the search infrastructure SRC-01/SRC-02 require. After this plan, `pnpm dev` opens a working site at localhost:4321 with the chrome in place — even though content collections (plan 01-04) and custom components (plan 01-05) come next.

Purpose: Land the framework + theme + search wiring as a single coherent step. Decoupling them would force later plans to re-discover Starlight quirks (sidebar config shape, Tailwind v4 + Starlight CSS variable bridging, Mermaid SSR pitfall). The UI-SPEC encodes every visual decision; this plan implements it verbatim.

Output: A runnable Astro site at `pnpm --filter @aigov/site dev` showing the 12-stage spine sidebar, working ⌘K Pagefind search, theme toggle (auto/light/dark), no console errors, no web-font requests in the Network tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md
@/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-UI-SPEC.md
@/Users/anand/AIgov/.planning/research/STACK.md
@/Users/anand/AIgov/.planning/research/ARCHITECTURE.md
@/Users/anand/AIgov/CLAUDE.md
@/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-01-PLAN.md
@/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/site with Astro 6 + Starlight 0.39 + Tailwind v4 + Mermaid + Icon integrations</name>
  <files>/Users/anand/AIgov/apps/site/package.json, /Users/anand/AIgov/apps/site/tsconfig.json, /Users/anand/AIgov/apps/site/astro.config.mjs, /Users/anand/AIgov/apps/site/src/env.d.ts, /Users/anand/AIgov/apps/site/public/favicon.svg, /Users/anand/AIgov/apps/site/public/.nojekyll, /Users/anand/AIgov/apps/site/src/pages/index.astro, /Users/anand/AIgov/apps/site/src/pages/_placeholder.md</files>
  <read_first>
    - /Users/anand/AIgov/.planning/research/STACK.md (Core Technologies table — Astro ^6.0, Starlight ^0.39, Tailwind ^4.1, plus Supporting Libraries `astro-mermaid`, `@astrojs/mdx`, `pagefind`; Version Compatibility section — `@tailwindcss/vite` plugin not PostCSS, Node 22)
    - /Users/anand/AIgov/.planning/research/ARCHITECTURE.md (Repository Layout section — apps/site directory tree, lines 100-160)
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-UI-SPEC.md (entire file — visual contract; especially Design System table, Tailwind v4 Theme Override block at lines 494-563, sidebar 12-stage list, accessibility requirements, density/print/dark-mode contracts)
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md (Visual Shell decisions — no web fonts, slate-indigo accent, comfortable density)
    - /Users/anand/AIgov/packages/shared/src/index.ts (workspace import — site depends on @aigov/shared)
    - /Users/anand/AIgov/tsconfig.base.json (extends pattern)
  </read_first>
  <action>
    Step 1 — Create `/Users/anand/AIgov/apps/site/package.json` with exact pinned dependencies (use `save-exact=true` from plan 01-01). Required:
    - `"name": "@aigov/site"`, `"version": "0.0.0"`, `"private": true`, `"type": "module"`
    - `"scripts": { "dev": "astro dev", "build": "astro build", "preview": "astro preview", "typecheck": "astro check", "astro": "astro" }`
    - `"dependencies"` (pin exact, do not use ^): `"astro": "6.0.x"`, `"@astrojs/starlight": "0.39.x"`, `"@astrojs/starlight-tailwind": "4.0.x"` (or current compat version), `"@astrojs/mdx": "4.0.x"`, `"tailwindcss": "4.1.x"`, `"@tailwindcss/vite": "4.1.x"`, `"astro-mermaid": "latest-pinned"` (resolve via `pnpm view astro-mermaid version`), `"astro-icon": "latest-pinned"`, `"@iconify-json/lucide": "latest-pinned"`, `"sharp": "0.33.x"` (Astro image optimization), `"@aigov/shared": "workspace:*"`
    - `"devDependencies"`: `"@astrojs/check": "0.9.x"`, `"typescript": "5.6.3"` (matches root)

    Run `pnpm install` from repo root after writing. If `astro-mermaid` or `@astrojs/starlight-tailwind` resolve issues arise (versions may need adjustment for Astro 6 / Starlight 0.39 compat), use `pnpm view <package> versions --json` to pick a compatible version. Document any version overrides in the SUMMARY.

    Step 2 — Create `/Users/anand/AIgov/apps/site/tsconfig.json` extending the base: `{ "extends": "astro/tsconfigs/strict", "include": [".astro/types.d.ts", "**/*"], "exclude": ["dist"], "compilerOptions": { "baseUrl": ".", "paths": { "@aigov/shared": ["../../packages/shared/src/index.ts"], "@aigov/shared/*": ["../../packages/shared/src/*"], "~/*": ["./src/*"] } } }`. The `~/*` alias matches the convention in ARCHITECTURE.md MDX imports (`import PersonaSection from '~/components/PersonaSection.astro'`).

    Step 3 — Create `/Users/anand/AIgov/apps/site/src/env.d.ts` with the single line `/// <reference path="../.astro/types.d.ts" />`.

    Step 4 — Create `/Users/anand/AIgov/apps/site/public/.nojekyll` (empty file). Required so GitHub Pages does not run Jekyll over Astro output (would break `_astro/` directory naming).

    Step 5 — Create `/Users/anand/AIgov/apps/site/public/favicon.svg` as a hand-authored 32×32 inline SVG. Use a simple geometric mark in the slate-indigo accent color: a 12-segment ring (mirroring the 12-stage journey) at `#4f46e5` on transparent. The exact SVG body must be ≤ 1KB. No external font references. This file is committed; downstream Phase 6 (TAD-06) may replace it with a polished design.

    Step 6 — Create `/Users/anand/AIgov/apps/site/astro.config.mjs`:
    ```javascript
    import { defineConfig } from 'astro/config';
    import starlight from '@astrojs/starlight';
    import mdx from '@astrojs/mdx';
    import tailwindcss from '@tailwindcss/vite';
    import mermaid from 'astro-mermaid';
    import icon from 'astro-icon';

    export default defineConfig({
      site: 'https://SITE_DOMAIN', // placeholder, real domain set in apps/site/src/config/site.ts in plan 01-06
      output: 'static',
      trailingSlash: 'always',
      build: { format: 'directory' },
      vite: { plugins: [tailwindcss()] },
      integrations: [
        mermaid({ theme: 'neutral', autoTheme: true }),
        icon({ include: { lucide: ['*'] } }),
        mdx(),
        starlight({
          title: 'AI Governance',
          description: 'Plain-language AI governance, current to this week.',
          customCss: ['./src/styles/global.css'],
          components: {
            // overrides land in plan 01-05 (PersonaSwitch, DisclaimerBanner, SustainabilityNotice)
          },
          social: [], // empty in Phase 1; populated in Phase 6 TAD-03
          pagination: true,
          tableOfContents: { minHeadingLevel: 2, maxHeadingLevel: 3 },
          lastUpdated: false, // disabled in Phase 1; FND-13 surfaces last-touched on per-stage cards instead
          editLink: { baseUrl: 'https://github.com/PLACEHOLDER_ORG/AIgov/edit/main/apps/site/' }, // wired with real org in Phase 6
          favicon: '/favicon.svg',
          sidebar: [
            {
              label: 'AI Governance Journey',
              items: [
                { label: '1. AI Policy', link: '/stages/ai-policy/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '2. Risk Tiering', link: '/stages/risk-tiering/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '3. Risk Check', link: '/stages/risk-check/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '4. Compliance', link: '/stages/compliance/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '5. Third-party AI Risk', link: '/stages/third-party-risk/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '6. Data Controls', link: '/stages/data-controls/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '7. Continuous Red-teaming', link: '/stages/red-teaming/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '8. Documentation', link: '/stages/documentation/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '9. Accountability', link: '/stages/accountability/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '10. Agentic AI Oversight', link: '/stages/agentic-oversight/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '11. Incident Response', link: '/stages/incident-response/', badge: { text: 'Phase 2', variant: 'note' } },
                { label: '12. Monitoring', link: '/stages/monitoring/', badge: { text: 'Phase 2', variant: 'note' } },
              ],
            },
            { label: 'About', link: '/about/' },
            { label: "What's new", link: '/whats-new/', badge: { text: 'Phase 3', variant: 'note' } },
            { label: 'Wizard', link: '/wizard/', badge: { text: 'Phase 4', variant: 'note' } },
          ],
          // Starlight's built-in Pagefind is enabled by default — SRC-01 (⌘K) + SRC-02 (indexes all content)
        }),
      ],
    });
    ```

    The 12-stage label list above matches CNT-01 (REQUIREMENTS.md) verbatim. Badges marked "Phase 2/3/4" make the placeholder status visible to the maintainer browsing the dev site.

    Step 7 — Create `/Users/anand/AIgov/apps/site/src/pages/index.astro` (Phase 1 landing page placeholder). It uses Starlight's `StarlightPage` component:
    ```astro
    ---
    import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
    ---
    <StarlightPage frontmatter={{ title: 'AI Governance — plain language, current weekly', template: 'splash', tableOfContents: false }}>
      <p>This site explains AI governance in plain language and tracks the world's authoritative regulations and vendor frameworks. Content authoring lands in Phase 2.</p>
      <p>The journey has 12 stages, from AI Policy through Monitoring. Use the sidebar to navigate or press <kbd>⌘K</kbd> (Mac) / <kbd>Ctrl+K</kbd> to search.</p>
      <p><strong>Status:</strong> Phase 1 — Foundation & Chrome. The shell is live; content authoring begins next phase.</p>
      <hr />
      <p><small>This site explains AI governance. It is not legal advice. Consult qualified counsel before acting on any content.</small></p>
    </StarlightPage>
    ```

    NOTE: the inline disclaimer here is the Phase 1 placeholder copy from UI-SPEC.md "Copywriting Contract" row "Disclaimer — footer". Final lock by lawyer in Phase 6 (TAD-01). The dedicated `<DisclaimerBanner>` component (plan 01-05) replaces this inline copy with a structured component once it lands.

    Step 8 — Create `/Users/anand/AIgov/apps/site/src/pages/_placeholder.md` containing one heading "Content collections land in plan 01-04" — this gives Pagefind a non-empty index so SRC-02 verification has something to find. The leading underscore prevents Astro from publishing it as a route.

    Step 9 — Run `pnpm install` from repo root, then `pnpm --filter @aigov/site build`. Build must succeed (with the placeholder index page; sidebar links to /stages/* will 404 in Phase 1 — that is expected per the "Phase 2" badges). If `astro check` complains about the missing stage routes, suppress only the route-404 warnings by ensuring sidebar links use the placeholder badges Starlight expects.
  </action>
  <verify>
    <automated>cd /Users/anand/AIgov && pnpm install --frozen-lockfile 2>&1 | tail -5 && test -f /Users/anand/AIgov/apps/site/astro.config.mjs && grep -q "starlight(" /Users/anand/AIgov/apps/site/astro.config.mjs && grep -q "tailwindcss()" /Users/anand/AIgov/apps/site/astro.config.mjs && grep -q "AI Policy" /Users/anand/AIgov/apps/site/astro.config.mjs && grep -q "Monitoring" /Users/anand/AIgov/apps/site/astro.config.mjs && test -f /Users/anand/AIgov/apps/site/public/.nojekyll && test -f /Users/anand/AIgov/apps/site/public/favicon.svg && pnpm --filter @aigov/site build 2>&1 | tee /tmp/aigov-site-build.log | grep -qE "(Complete|built in|✓)" && test -d /Users/anand/AIgov/apps/site/dist && test -f /Users/anand/AIgov/apps/site/dist/index.html && grep -q "AI Governance" /Users/anand/AIgov/apps/site/dist/index.html</automated>
  </verify>
  <done>
    - `pnpm install` succeeds with all pinned versions resolved
    - `pnpm --filter @aigov/site build` exits 0 and produces `dist/index.html`
    - `astro.config.mjs` lists exactly 12 stage labels in the sidebar (matches CNT-01 list verbatim)
    - Integrations registered: starlight, mdx, astro-mermaid, astro-icon (with lucide), Tailwind v4 via Vite plugin
    - `public/.nojekyll` and `public/favicon.svg` exist
    - Tailwind v4 is wired via `@tailwindcss/vite` plugin (NOT the legacy PostCSS plugin — STACK.md Version Compatibility requirement)
    - The site is browsable at `pnpm --filter @aigov/site dev` (manual confirm in next plan; this plan completes when build succeeds)
  </done>
</task>

<task type="auto">
  <name>Task 2: Author global.css with Tailwind v4 @theme block, system fonts, slate-indigo accent, reduced-motion default</name>
  <files>/Users/anand/AIgov/apps/site/src/styles/global.css</files>
  <read_first>
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-UI-SPEC.md (entire Color section lines 110-170, Typography section lines 70-110, Motion & Interaction section lines 170-220, Tailwind v4 Theme Override block lines 494-563 — implement this VERBATIM)
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md (Visual Shell decisions — no web fonts, slate-indigo, comfortable density)
    - /Users/anand/AIgov/.planning/research/STACK.md (Tailwind v4 note — no config file, CSS-first variables)
    - /Users/anand/AIgov/apps/site/astro.config.mjs (customCss path written in Task 1)
  </read_first>
  <action>
    Implement the global.css EXACTLY per UI-SPEC lines 494-563 (the "Tailwind v4 Theme Override (Implementation Reference)" block). Plus additional rules per UI-SPEC sections.

    Step 1 — Create `/Users/anand/AIgov/apps/site/src/styles/global.css` with these blocks IN ORDER:

    A. Imports (top of file):
    ```css
    @import "tailwindcss";
    @import "@astrojs/starlight-tailwind";
    ```

    B. `@theme` block — copy VERBATIM from UI-SPEC lines 502-538 (color-accent-50 through color-accent-950, color-gray-50 through color-gray-950, font-sans, font-mono, motion-fast/base/slow, motion-easing). No edits, no additions. These exact hex values are locked in CONTEXT.md.

    C. Starlight variable bridging (UI-SPEC lines 540-551 verbatim) for `:root` light theme and `:root[data-theme="dark"]` dark theme.

    D. Reduced-motion default (UI-SPEC lines 554-561 verbatim).

    E. Persona section visibility CSS (from UI-SPEC line 244 component contract — required so the persona switch component lands in plan 01-05 without re-deriving):
    ```css
    /* PersonaSection visibility — driven by data-persona on <html>. Default = all visible. */
    html[data-persona]:not([data-persona="all"]) section[data-for-persona]:not([data-for-persona="all"]) {
      display: none;
    }
    html[data-persona="exec"] section[data-for-persona="exec"],
    html[data-persona="engineer"] section[data-for-persona="engineer"],
    html[data-persona="compliance"] section[data-for-persona="compliance"],
    html:not([data-persona]) section[data-for-persona],
    html[data-persona="all"] section[data-for-persona] {
      display: block;
    }
    ```

    F. JurisdictionLens visibility CSS (parallel structure for `data-jurisdiction` — UI-SPEC line 263):
    ```css
    html[data-jurisdiction]:not([data-jurisdiction="all"]) aside[data-for-jurisdiction]:not([data-for-jurisdiction="all"]) {
      display: none;
    }
    ```

    G. Focus ring (UI-SPEC line 216 — WCAG 2.4.7):
    ```css
    *:focus-visible {
      outline: 2px solid var(--sl-color-accent);
      outline-offset: 2px;
      border-radius: 4px;
    }
    ```

    H. Skip-to-content link (UI-SPEC line 217):
    ```css
    .skip-link {
      position: absolute;
      left: -9999px;
      top: 0;
    }
    .skip-link:focus-visible {
      left: 1rem;
      top: 1rem;
      z-index: 9999;
      padding: 0.5rem 1rem;
      background: var(--sl-color-bg);
      color: var(--sl-color-text);
      border: 2px solid var(--sl-color-accent);
      border-radius: 4px;
    }
    ```

    I. Print stylesheet (UI-SPEC "Print Stylesheet" section lines 430-454 verbatim — the `@media print` block).

    J. Touch-target minimum (UI-SPEC line 66 — WCAG 2.5.8):
    ```css
    .touch-target,
    button[aria-label]:not(.no-min):not(:has(span)),
    [role="button"][aria-label]:not(.no-min) {
      min-height: 2.75rem; /* 44px */
      min-width: 2.75rem;
    }
    ```

    Step 2 — Verify the file is ≥ 100 lines and contains all the required selectors via the verify command. Make sure `@import "tailwindcss";` is line 1 (Tailwind v4 requirement — the import order matters because @theme must come after it).

    Step 3 — Re-run `pnpm --filter @aigov/site build` and confirm Tailwind v4 picks up the @theme block. The build log should NOT show "missing tailwind config" warnings (Tailwind v4 is CSS-first; no tailwind.config.js).

    Step 4 — Manually verify by inspecting the built CSS file in `apps/site/dist/_astro/*.css`: search for `--color-accent-600: #4f46e5` (the primary accent token) and `--font-sans:` containing `system-ui`. If either is absent, the @theme block did not register and the import order is wrong.
  </action>
  <verify>
    <automated>test -f /Users/anand/AIgov/apps/site/src/styles/global.css && head -1 /Users/anand/AIgov/apps/site/src/styles/global.css | grep -q '@import "tailwindcss"' && grep -q "@import \"@astrojs/starlight-tailwind\"" /Users/anand/AIgov/apps/site/src/styles/global.css && grep -q "color-accent-600: #4f46e5" /Users/anand/AIgov/apps/site/src/styles/global.css && grep -q "system-ui" /Users/anand/AIgov/apps/site/src/styles/global.css && grep -q "prefers-reduced-motion: reduce" /Users/anand/AIgov/apps/site/src/styles/global.css && grep -q "data-for-persona" /Users/anand/AIgov/apps/site/src/styles/global.css && grep -q "@media print" /Users/anand/AIgov/apps/site/src/styles/global.css && grep -q "skip-link" /Users/anand/AIgov/apps/site/src/styles/global.css && grep -q ":focus-visible" /Users/anand/AIgov/apps/site/src/styles/global.css && wc -l /Users/anand/AIgov/apps/site/src/styles/global.css | awk '{if ($1 < 100) exit 1}' && cd /Users/anand/AIgov && pnpm --filter @aigov/site build 2>&1 | grep -qE "(Complete|built in|✓)" && grep -r "color-accent-600" /Users/anand/AIgov/apps/site/dist/_astro/ 2>/dev/null | head -1 | grep -q "4f46e5"</automated>
  </verify>
  <done>
    - `global.css` exists, ≥100 lines, starts with `@import "tailwindcss";`
    - All UI-SPEC color tokens present (accent-50..950, gray-50..950 — verified by grep on key hexes)
    - System-font stack with no web font imports anywhere in the file (no `@import url("https://fonts...")`)
    - Reduced-motion default applied
    - PersonaSection + JurisdictionLens visibility CSS in place (so plan 01-05 components can drop in without further CSS work)
    - Focus ring, skip-link, touch-target, print stylesheet all present
    - Build produces a CSS file containing the @theme variables (confirmed by grep on `#4f46e5` in `dist/_astro/*.css`)
    - No web-font network requests when the site is loaded (manually verifiable in Network tab; automated check is the grep above)
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @aigov/site build` exits 0 with no warnings about missing integrations or Tailwind config
- `apps/site/dist/index.html` contains "AI Governance" in title and the Phase 1 placeholder copy
- `apps/site/dist/pagefind/` directory exists (Starlight 0.39 builds the Pagefind index automatically)
- The 12 stage sidebar items appear in the built HTML (each with the "Phase 2" badge)
- DevTools Network tab on a dev-server load shows ZERO requests to fonts.googleapis.com, fonts.gstatic.com, or any external CDN
- Starlight's built-in theme toggle and search modal render in the header
</verification>

<success_criteria>
- FND-02: Astro 6 + Starlight 0.39+ + Tailwind v4 (`@tailwindcss/vite`) site exists at `apps/site`, builds successfully, runs on Node 22
- FND-06 (placeholder portion): Sidebar shows the 12-stage spine on every page (final Mermaid landing-page visual is Phase 2 CNT-09; the Mermaid integration is wired here, the placeholder lands in plan 01-05's `<MermaidJourney>`)
- SRC-01: Pagefind search modal opens with ⌘K (Starlight built-in)
- SRC-02: Pagefind index is built into `dist/pagefind/` covering all routes (will pick up stage/regulation/vendor/glossary pages once plan 01-04 adds the collections)
- No web fonts fetched (verified via CSS grep and Network tab discipline)
- Dark mode toggle in header respects `prefers-color-scheme: auto` by default
- The visual contract in UI-SPEC is implementable by plans 01-05/01-06 because the CSS theme tokens are already in place
</success_criteria>

<output>
After completion, create `/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-03-SUMMARY.md` documenting: exact pinned versions for astro/starlight/tailwind/astro-mermaid/astro-icon, the 12 sidebar labels, key file paths, any version-conflict adjustments made vs the plan, and the build output size (`dist/` size baseline for FND-10 weight-budget tuning).
</output>
