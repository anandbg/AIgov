---
phase: 01-foundation-chrome
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/anand/AIgov/package.json
  - /Users/anand/AIgov/pnpm-workspace.yaml
  - /Users/anand/AIgov/tsconfig.base.json
  - /Users/anand/AIgov/packages/shared/package.json
  - /Users/anand/AIgov/packages/shared/tsconfig.json
  - /Users/anand/AIgov/packages/shared/src/index.ts
  - /Users/anand/AIgov/packages/shared/src/schemas/stage.ts
  - /Users/anand/AIgov/packages/shared/src/schemas/regulation.ts
  - /Users/anand/AIgov/packages/shared/src/schemas/snapshot.ts
  - /Users/anand/AIgov/packages/shared/src/schemas/vendor.ts
  - /Users/anand/AIgov/packages/shared/src/schemas/glossary.ts
  - /Users/anand/AIgov/packages/shared/src/schemas/story.ts
  - /Users/anand/AIgov/packages/shared/src/schemas/index.ts
  - /Users/anand/AIgov/packages/shared/src/types.ts
  - /Users/anand/AIgov/packages/shared/src/urls.ts
  - /Users/anand/AIgov/packages/embed-cli/package.json
  - /Users/anand/AIgov/packages/embed-cli/src/index.ts
  - /Users/anand/AIgov/apps/site/package.json
  - /Users/anand/AIgov/apps/chat-worker/package.json
  - /Users/anand/AIgov/apps/pipeline/package.json
autonomous: true
requirements:
  - FND-01
must_haves:
  truths:
    - "pnpm-workspace.yaml declares all five workspaces (apps/site, apps/chat-worker, apps/pipeline, packages/shared, packages/embed-cli)"
    - "`pnpm install` from repo root succeeds and creates pnpm-lock.yaml"
    - "Workspace import `@aigov/shared` resolves via pnpm symlink (no publish step)"
    - "Zod schemas for all 5 content collection shapes (stage, regulation, snapshot, vendor, glossary, story) exist in packages/shared/src/schemas/ and are the single source of truth"
    - "TypeScript path aliases via tsconfig.base.json resolve @aigov/shared in apps and packages"
  artifacts:
    - path: "/Users/anand/AIgov/package.json"
      provides: "Root workspace manifest with pnpm packageManager pin and engines.node = 22"
      contains: "packageManager"
    - path: "/Users/anand/AIgov/pnpm-workspace.yaml"
      provides: "Workspace declaration listing all apps and packages"
      contains: "apps/*"
    - path: "/Users/anand/AIgov/tsconfig.base.json"
      provides: "Shared TypeScript config — strict, ES2022 target, NodeNext module"
    - path: "/Users/anand/AIgov/packages/shared/src/schemas/stage.ts"
      provides: "Zod schema for stage collection frontmatter (StageFrontmatter, RegulationRef)"
      contains: "z.object"
    - path: "/Users/anand/AIgov/packages/shared/src/schemas/index.ts"
      provides: "Barrel export of all schemas"
    - path: "/Users/anand/AIgov/packages/shared/src/urls.ts"
      provides: "Canonical URL builders used across site/worker/pipeline"
  key_links:
    - from: "apps/site/package.json"
      to: "packages/shared (via workspace:*)"
      via: "pnpm workspace dependency"
      pattern: "\"@aigov/shared\": \"workspace:\\*\""
    - from: "packages/shared/src/index.ts"
      to: "all schemas + types + urls"
      via: "barrel export"
      pattern: "export \\* from"
threat_model:
  surfaces:
    - "pnpm dependency resolution (supply chain)"
    - "Workspace package linking"
  mitigations:
    - "save-exact=true (set in plan 01-01) pins all versions; no ranges"
    - "engine-strict=true enforces Node 22 across all workspaces"
    - "packageManager field locks pnpm version (prevents accidental npm/yarn)"
    - "No runtime dependencies in packages/shared (zod is dev-only at this stage; production runtime added by consumers)"
---

<objective>
Build the pnpm monorepo skeleton: root workspace, five workspaces (`apps/site`, `apps/chat-worker`, `apps/pipeline`, `packages/shared`, `packages/embed-cli`), shared TypeScript base config, and the Zod schema source-of-truth for all five content collections (`stages`, `regulations`, `vendor`, `glossary`, `stories`) + URL builders. This is the FND-01 backbone that every later plan imports from.

Purpose: Establish the workspace topology and the single-source-of-truth Zod schemas (per ARCHITECTURE.md "Pattern 1: Frontmatter as Schema-Validated Contract") so that plan 01-04 (content collections) and all Phase 2/3/4/5 plans can import `@aigov/shared` without copy-pasting schemas.

Output: A monorepo where `pnpm install` succeeds, `@aigov/shared` resolves in any workspace via `workspace:*`, and the five Zod schemas + URL builders are exported from `packages/shared`. Apps directories exist as stubs (real scaffolding lands in plans 01-03 through 01-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/anand/AIgov/.planning/PROJECT.md
@/Users/anand/AIgov/.planning/research/ARCHITECTURE.md
@/Users/anand/AIgov/.planning/research/STACK.md
@/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md
@/Users/anand/AIgov/CLAUDE.md

<interfaces>
<!-- The Zod schema shapes this plan creates. Downstream plans (01-04 especially) import these. -->
<!-- Schema source: ARCHITECTURE.md "Topic ↔ Regulation Bidirectional Matrix" section, plus collection list from FND-04 -->

Five content collections (FND-04 — exactly five):
- `stages` — 12 journey topics; canonical narrative; persona + regulation refs
- `regulations` — one folder per source with `index.md` + dated `snapshots/`
- `vendor` — same shape as regulations but for vendor policy docs
- `glossary` — one term per file (60+ in Phase 2)
- `stories` — fictional companies (Sigma Health, Aurora Insurance, etc.)

Zod patterns required (from ARCHITECTURE.md lines 263-289):
```typescript
export const RegulationRef = z.object({
  source: z.string(),
  articles: z.array(z.string()),
  relevance: z.enum(['core', 'supporting', 'related']),
  note: z.string().optional()
});

export const StageFrontmatter = z.object({
  stage: z.number().int().min(0).max(12),
  slug: z.string(),
  title: z.string(),
  subtitle: z.string().optional(),
  storyCharacter: z.string().optional(),
  personas: z.object({ exec: z.boolean().default(true), engineer: z.boolean().default(true), compliance: z.boolean().default(true) }),
  regulations: z.array(RegulationRef),
  prerequisites: z.array(z.string()).default([]),
  lastMeaningfulChange: z.string().optional(),
  draft: z.boolean().default(false)
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold pnpm workspace skeleton + root package.json + tsconfig.base.json</name>
  <files>/Users/anand/AIgov/package.json, /Users/anand/AIgov/pnpm-workspace.yaml, /Users/anand/AIgov/tsconfig.base.json, /Users/anand/AIgov/apps/chat-worker/package.json, /Users/anand/AIgov/apps/pipeline/package.json, /Users/anand/AIgov/packages/embed-cli/package.json, /Users/anand/AIgov/packages/embed-cli/src/index.ts</files>
  <read_first>
    - /Users/anand/AIgov/.planning/research/ARCHITECTURE.md (Repository Layout section, lines 96-242 — exact directory tree)
    - /Users/anand/AIgov/.planning/research/STACK.md (Version Compatibility table — Node >=20.19 for Astro 6, pnpm as package manager)
    - /Users/anand/AIgov/.nvmrc (created in plan 01-01 — Node 22)
    - /Users/anand/AIgov/.npmrc (created in plan 01-01 — engine-strict=true, save-exact=true)
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md (content-collection identifiers section: exactly five collections)
  </read_first>
  <action>
    Step 1 — Determine the latest stable pnpm 9.x version. Run `corepack prepare pnpm@latest --activate` then capture `pnpm --version` output. Pin that exact version (e.g., `9.15.0`) in subsequent files. Do not use a range — `save-exact=true` is set.

    Step 2 — Create `/Users/anand/AIgov/pnpm-workspace.yaml` with exactly:
    ```
    packages:
      - 'apps/*'
      - 'packages/*'
    ```

    Step 3 — Create `/Users/anand/AIgov/package.json` (the ROOT manifest). Required fields:
    - `"name": "aigov"`
    - `"version": "0.0.0"`
    - `"private": true`
    - `"packageManager": "pnpm@<pinned-version-from-step-1>"`
    - `"engines": { "node": ">=22.0.0 <23.0.0", "pnpm": ">=9.0.0" }`
    - `"workspaces"` — NOT set here; pnpm uses pnpm-workspace.yaml instead
    - `"scripts"`: `"build": "pnpm -r run build"`, `"dev": "pnpm --filter @aigov/site dev"`, `"typecheck": "pnpm -r run typecheck"`, `"lint": "pnpm -r run lint"`, `"test": "pnpm -r run test"`, `"clean": "pnpm -r exec rm -rf node_modules dist .astro .turbo"`
    - `"devDependencies"`: pin `"typescript": "5.6.3"` (or latest 5.6.x), `"@types/node": "22.10.1"` (or latest 22.x), `"prettier": "3.4.2"` (or latest 3.4.x)

    Do NOT add zod, astro, hono, or any runtime dep at root — those land in their respective workspace packages.

    Step 4 — Create `/Users/anand/AIgov/tsconfig.base.json` with: `"compilerOptions": { "target": "ES2022", "module": "NodeNext", "moduleResolution": "NodeNext", "strict": true, "esModuleInterop": true, "skipLibCheck": true, "forceConsistentCasingInFileNames": true, "resolveJsonModule": true, "isolatedModules": true, "noEmit": true, "verbatimModuleSyntax": true, "noUncheckedIndexedAccess": true, "noImplicitOverride": true, "lib": ["ES2022"], "paths": { "@aigov/shared": ["./packages/shared/src/index.ts"], "@aigov/shared/*": ["./packages/shared/src/*"] } }`. The `paths` aliases let any workspace import `@aigov/shared` for editor IntelliSense.

    Step 5 — Create empty placeholder directories for downstream plans (so workspace globbing works):
    - `/Users/anand/AIgov/apps/site/` (plan 01-03 fills it; for now create a stub package.json)
    - `/Users/anand/AIgov/apps/chat-worker/` (Phase 5 fills it; Phase 1 only declares the workspace)
    - `/Users/anand/AIgov/apps/pipeline/` (Phase 3 fills it; Phase 1 only declares the workspace)
    - `/Users/anand/AIgov/packages/shared/`
    - `/Users/anand/AIgov/packages/embed-cli/`

    For `apps/chat-worker/package.json` and `apps/pipeline/package.json`, create minimal stubs:
    - chat-worker: `{ "name": "@aigov/chat-worker", "version": "0.0.0", "private": true, "type": "module", "scripts": { "build": "echo 'chat-worker scaffolds in Phase 5'", "typecheck": "echo 'pending Phase 5'" } }`
    - pipeline: `{ "name": "@aigov/pipeline", "version": "0.0.0", "private": true, "type": "module", "scripts": { "build": "echo 'pipeline scaffolds in Phase 3'", "typecheck": "echo 'pending Phase 3'" } }`

    For `packages/embed-cli/package.json`: `{ "name": "@aigov/embed-cli", "version": "0.0.0", "private": true, "type": "module", "scripts": { "build": "echo 'embed-cli scaffolds in Phase 5'", "typecheck": "echo 'pending Phase 5'" } }`. Also create a placeholder `src/index.ts` with a single comment line: `// Embed CLI implementation lands in Phase 5 (CHT-02). See .planning/research/ARCHITECTURE.md`.

    `apps/site/package.json` is created in plan 01-03 — DO NOT create it here.

    Step 6 — Run `pnpm install` from `/Users/anand/AIgov/`. Should succeed and produce `pnpm-lock.yaml`. Commit that lockfile in this plan (downstream plans add to it).
  </action>
  <verify>
    <automated>test -f /Users/anand/AIgov/pnpm-workspace.yaml && grep -q "apps/\*" /Users/anand/AIgov/pnpm-workspace.yaml && grep -q "packages/\*" /Users/anand/AIgov/pnpm-workspace.yaml && test -f /Users/anand/AIgov/package.json && node -e "const p=require('/Users/anand/AIgov/package.json'); if(!p.packageManager.startsWith('pnpm@')) process.exit(1); if(!p.engines || !p.engines.node) process.exit(1); if(p.name !== 'aigov') process.exit(1);" && test -f /Users/anand/AIgov/tsconfig.base.json && node -e "const t=require('/Users/anand/AIgov/tsconfig.base.json'); if(!t.compilerOptions.strict) process.exit(1); if(t.compilerOptions.target !== 'ES2022') process.exit(1);" && test -f /Users/anand/AIgov/pnpm-lock.yaml && test -d /Users/anand/AIgov/apps/chat-worker && test -d /Users/anand/AIgov/apps/pipeline && test -d /Users/anand/AIgov/packages/shared && test -d /Users/anand/AIgov/packages/embed-cli</automated>
  </verify>
  <done>
    - `pnpm install` exits 0 with no warnings about workspace globs
    - `pnpm-workspace.yaml` lists `apps/*` and `packages/*`
    - Root `package.json` pins pnpm version via `packageManager` and Node 22 via `engines`
    - `tsconfig.base.json` has `strict: true`, `noUncheckedIndexedAccess: true`, ES2022 target
    - Workspace directories exist for all five workspaces (apps/chat-worker, apps/pipeline, packages/shared, packages/embed-cli stubs created; apps/site directory exists empty — plan 01-03 fills it)
  </done>
</task>

<task type="auto">
  <name>Task 2: Author @aigov/shared package — Zod schemas for all 5 content collections + URL builders</name>
  <files>/Users/anand/AIgov/packages/shared/package.json, /Users/anand/AIgov/packages/shared/tsconfig.json, /Users/anand/AIgov/packages/shared/src/index.ts, /Users/anand/AIgov/packages/shared/src/schemas/stage.ts, /Users/anand/AIgov/packages/shared/src/schemas/regulation.ts, /Users/anand/AIgov/packages/shared/src/schemas/snapshot.ts, /Users/anand/AIgov/packages/shared/src/schemas/vendor.ts, /Users/anand/AIgov/packages/shared/src/schemas/glossary.ts, /Users/anand/AIgov/packages/shared/src/schemas/story.ts, /Users/anand/AIgov/packages/shared/src/schemas/index.ts, /Users/anand/AIgov/packages/shared/src/types.ts, /Users/anand/AIgov/packages/shared/src/urls.ts</files>
  <read_first>
    - /Users/anand/AIgov/.planning/research/ARCHITECTURE.md (Topic↔Regulation Bidirectional Matrix section, lines 256-340 — exact RegulationRef and StageFrontmatter shapes; Wizard Architecture section lines 576-647 for future schema additions; Regulation Snapshot Storage section lines 340-389 for snapshot frontmatter)
    - /Users/anand/AIgov/.planning/research/STACK.md (Supporting Libraries — `zod ^3.23`)
    - /Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-CONTEXT.md (Specific Ideas — content-collection identifiers: `stages`, `regulations`, `vendor`, `glossary`, `stories` per FND-04, exactly five)
    - /Users/anand/AIgov/CLAUDE.md (TypeScript Coding Style sections — immutability with spread, Zod validation)
    - /Users/anand/.claude/rules/typescript/coding-style.md (Zod patterns)
  </read_first>
  <action>
    Step 1 — Create `/Users/anand/AIgov/packages/shared/package.json`:
    - `"name": "@aigov/shared"`
    - `"version": "0.0.0"`
    - `"private": true`
    - `"type": "module"`
    - `"main": "./src/index.ts"`
    - `"types": "./src/index.ts"`
    - `"exports": { ".": "./src/index.ts", "./schemas": "./src/schemas/index.ts", "./urls": "./src/urls.ts", "./types": "./src/types.ts" }`
    - `"scripts": { "typecheck": "tsc --noEmit -p tsconfig.json" }`
    - `"dependencies": { "zod": "3.23.8" }` (pin exact; latest 3.23.x as of 2026-05)

    Step 2 — Create `/Users/anand/AIgov/packages/shared/tsconfig.json` extending the base: `{ "extends": "../../tsconfig.base.json", "compilerOptions": { "rootDir": "./src", "outDir": "./dist" }, "include": ["src/**/*"] }`.

    Step 3 — Create `/Users/anand/AIgov/packages/shared/src/schemas/stage.ts`:
    Export `RegulationRef` and `StageFrontmatter` exactly as specified in ARCHITECTURE.md lines 263-289 (reproduced above in `<interfaces>`). Use Zod v3 API: `z.object`, `z.string`, `z.array`, `z.enum`, `z.boolean().default(true)`, `z.number().int().min(0).max(12)`. Export both the schema (`StageFrontmatter`) and the inferred TypeScript type via `export type Stage = z.infer<typeof StageFrontmatter>`. Same for `RegulationRef` → `RegulationRefT`.

    Step 4 — Create `/Users/anand/AIgov/packages/shared/src/schemas/regulation.ts`:
    Schema for `regulations/<source>/index.md` frontmatter. Fields:
    - `source: z.string()` (matches folder name, e.g., "eu-ai-act")
    - `name: z.string()` (display name, e.g., "EU AI Act")
    - `kind: z.enum(['regulation', 'framework', 'standard', 'guideline'])`
    - `jurisdiction: z.enum(['eu', 'uk', 'us', 'us-state', 'global', 'apac'])`
    - `canonicalUrl: z.string().url()`
    - `firstTrackedAt: z.string()` (ISO date when site began tracking)
    - `currentSnapshotDate: z.string().optional()` (latest snapshot file date)
    - `description: z.string()` (1-2 sentence summary)
    Export `Regulation = z.infer<typeof RegulationFrontmatter>`.

    Step 5 — Create `/Users/anand/AIgov/packages/shared/src/schemas/snapshot.ts`:
    Schema for `regulations/<source>/snapshots/<YYYY-MM-DD>.md` frontmatter. Per ARCHITECTURE.md lines 372-388:
    - `source: z.string()`
    - `snapshotDate: z.string()` (ISO date)
    - `srcUrl: z.string().url()`
    - `fetchedAt: z.string()` (ISO datetime)
    - `fetcherVersion: z.string()` (e.g., "pipeline-0.4.2")
    - `materialChange: z.boolean()`
    - `changeKind: z.enum(['editorial', 'clarification', 'amendment', 'new-section'])`
    - `changeSummary: z.string()`
    - `relatedArticles: z.array(z.string()).default([])`
    Export `Snapshot = z.infer<typeof SnapshotFrontmatter>`.

    Step 6 — Create `/Users/anand/AIgov/packages/shared/src/schemas/vendor.ts`:
    Schema for vendor policy collection (same shape as regulation but `kind: z.literal('vendor-policy')` and `vendor: z.string()`). Fields: `vendor`, `name`, `policyType` (`usage-policy | terms | api-tos | acceptable-use`), `canonicalUrl`, `firstTrackedAt`, `currentSnapshotDate?`, `description`.

    Step 7 — Create `/Users/anand/AIgov/packages/shared/src/schemas/glossary.ts`:
    Schema for `glossary/*.md`. Fields:
    - `term: z.string()` (display term)
    - `slug: z.string()` (URL-safe)
    - `aliases: z.array(z.string()).default([])` (alternate phrasings the auto-wrapper should match)
    - `definition: z.string().max(320)` (cap matches UI-SPEC tooltip constraint)
    - `related: z.array(z.string()).default([])` (other term slugs)
    - `seeAlso: z.array(z.object({ kind: z.enum(['stage', 'regulation', 'vendor']), slug: z.string() })).default([])`
    Export `Glossary = z.infer<typeof GlossaryFrontmatter>`.

    Step 8 — Create `/Users/anand/AIgov/packages/shared/src/schemas/story.ts`:
    Schema for fictional company `stories/*.md`. Fields:
    - `company: z.string()` (e.g., "Sigma Health")
    - `slug: z.string()`
    - `jurisdiction: z.enum(['eu', 'uk', 'us', 'us-state', 'apac', 'global'])`
    - `industry: z.string()`
    - `protagonist: z.object({ name: z.string(), role: z.string() })`
    - `description: z.string()` (1-2 sentence elevator pitch for the company)
    - `useCases: z.array(z.string()).default([])` (AI use cases that pop up across stages)
    Export `Story = z.infer<typeof StoryFrontmatter>`.

    Step 9 — Create `/Users/anand/AIgov/packages/shared/src/schemas/index.ts`:
    Barrel file. `export * from './stage.js'; export * from './regulation.js'; export * from './snapshot.js'; export * from './vendor.js'; export * from './glossary.js'; export * from './story.js';` (`.js` extension required for NodeNext ESM resolution).

    Step 10 — Create `/Users/anand/AIgov/packages/shared/src/urls.ts`:
    Canonical URL builders. Pure functions, no side effects, no imports. Export each as a named const arrow function:
    - `stageUrl(slug: string): string` → returns `/stages/${slug}/`
    - `regulationUrl(source: string): string` → returns `/regulations/${source}/`
    - `snapshotUrl(source: string, date: string): string` → returns `/regulations/${source}/${date}/` (date is ISO YYYY-MM-DD)
    - `vendorUrl(vendor: string): string` → returns `/vendor/${vendor}/`
    - `glossaryUrl(slug: string): string` → returns `/glossary/${slug}/`
    - `storyUrl(slug: string): string` → returns `/stories/${slug}/`
    - `articleAnchor(article: string): string` → kebab-cases the article identifier and returns `#${kebab}` (e.g., "Art-11" → "#art-11", "Annex-IV" → "#annex-iv")
    All functions are total (no nulls); empty input is acceptable but undefined behavior — document with JSDoc.

    Step 11 — Create `/Users/anand/AIgov/packages/shared/src/types.ts`:
    Re-export every `z.infer` type from schemas as a convenience. Example:
    ```
    export type { Stage, RegulationRefT } from './schemas/stage.js';
    export type { Regulation } from './schemas/regulation.js';
    export type { Snapshot } from './schemas/snapshot.js';
    export type { Vendor } from './schemas/vendor.js';
    export type { Glossary } from './schemas/glossary.js';
    export type { Story } from './schemas/story.js';
    ```

    Step 12 — Create `/Users/anand/AIgov/packages/shared/src/index.ts` (the package entry):
    ```
    export * from './schemas/index.js';
    export * from './urls.js';
    export type * from './types.js';
    ```

    Step 13 — Run `pnpm install` from root to wire workspace symlinks, then `pnpm --filter @aigov/shared typecheck` to validate the package compiles. Fix any type errors in schema files before declaring done.
  </action>
  <verify>
    <automated>cd /Users/anand/AIgov && pnpm install --frozen-lockfile 2>&1 | grep -qE "(Lockfile is up to date|Done)" && pnpm --filter @aigov/shared typecheck 2>&1 && node -e "import('/Users/anand/AIgov/packages/shared/src/urls.ts').then(m => { if(m.stageUrl('risk-tiering') !== '/stages/risk-tiering/') throw new Error('stageUrl wrong'); if(m.articleAnchor('Art-11') !== '#art-11') throw new Error('articleAnchor wrong'); if(m.snapshotUrl('eu-ai-act','2026-05-22') !== '/regulations/eu-ai-act/2026-05-22/') throw new Error('snapshotUrl wrong'); console.log('OK'); }).catch(e => { console.error(e); process.exit(1); })" 2>/dev/null || (cd /Users/anand/AIgov && npx tsx -e "import {stageUrl, articleAnchor, snapshotUrl} from './packages/shared/src/urls.ts'; if(stageUrl('risk-tiering') !== '/stages/risk-tiering/') process.exit(1); if(articleAnchor('Art-11') !== '#art-11') process.exit(1); if(snapshotUrl('eu-ai-act','2026-05-22') !== '/regulations/eu-ai-act/2026-05-22/') process.exit(1);")</automated>
  </verify>
  <done>
    - `pnpm --filter @aigov/shared typecheck` exits 0 (no TS errors)
    - All 6 schema files exist under `packages/shared/src/schemas/` (stage, regulation, snapshot, vendor, glossary, story)
    - `packages/shared/src/index.ts` re-exports schemas, urls, and types
    - URL builders return the documented paths (`stageUrl('risk-tiering')` → `/stages/risk-tiering/`, `articleAnchor('Art-11')` → `#art-11`)
    - Each schema exports both the Zod schema AND the inferred TS type
    - `zod` is pinned to `3.23.8` exact in packages/shared/package.json
    - Workspace symlink exists at `/Users/anand/AIgov/node_modules/@aigov/shared` pointing to `packages/shared`
  </done>
</task>

</tasks>

<verification>
- `pnpm install` from repo root completes with no errors and produces `pnpm-lock.yaml`
- `pnpm -r run typecheck` runs typecheck across all workspaces (currently only @aigov/shared has real code; others are stubs with echo scripts)
- `ls /Users/anand/AIgov/node_modules/@aigov/shared` shows a symlink, not a copy
- Schemas are importable: `node --experimental-strip-types -e "import { StageFrontmatter } from '/Users/anand/AIgov/packages/shared/src/index.ts'; StageFrontmatter.parse({ stage: 1, slug: 'test', title: 'T', personas: {}, regulations: [] })"` succeeds (defaults fill missing fields)
- URL builders are pure and deterministic
</verification>

<success_criteria>
- pnpm monorepo with five workspaces declared (apps/site, apps/chat-worker, apps/pipeline, packages/shared, packages/embed-cli)
- Root package.json pins pnpm via `packageManager` and Node 22 via `engines`
- `tsconfig.base.json` enforces strict + noUncheckedIndexedAccess + ES2022
- `@aigov/shared` package exports Zod schemas for all 5 content collections (FND-04 list) plus URL builders
- Schemas compile without TS errors and parse fixture data correctly
- Per ARCHITECTURE.md "Pattern 1": packages/shared is the single source of truth — no schema duplication anywhere else in the repo
- FND-01 requirement satisfied
</success_criteria>

<output>
After completion, create `/Users/anand/AIgov/.planning/phases/01-foundation-chrome/01-02-SUMMARY.md` listing: workspace topology, pinned versions (pnpm, TypeScript, zod, @types/node), schema file inventory (6 files), URL builder API surface, any deviations from ARCHITECTURE.md schemas.
</output>
